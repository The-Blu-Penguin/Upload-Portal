/**
 * This file is kept for historical reference only.
 *
 * The project uses Tailwind CSS v4, which reads configuration directly
 * from globals.css via the `@import "tailwindcss"` and `@plugin` directives.
 * This v3-style JS config file is NOT processed by Tailwind v4 and can be
 * safely deleted.
 *
 * Content scanning in v4 is automatic.
 * The @tailwindcss/forms plugin is registered in globals.css via:
 *   @plugin "@tailwindcss/forms";
 */